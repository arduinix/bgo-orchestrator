schema: src/schema/schema.graphql  # Path to your GraphQL schema file
# documents:
#   - src/schema/schema.graphql  # Path to your GraphQL operations
generates:
  src/types/generated/graphql.ts:  # Output file for generated types
    plugins:
      - typescript
      - typescript-operations